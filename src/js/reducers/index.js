// import { combineReducers } from 'redux'

const initialState = {
  banana: false
}

export default function rootReducer (state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
